# Benchmark Fixtures

`qualitative_redteam_cases.yaml` defines safe, non-operational qualitative red-team cases used by:

- `python -m src.generate_demo_benchmark`
- `python -m src.run_redteam --config evals/config.yaml`

These cases are suitable for public demo mode and do not require external API calls.
